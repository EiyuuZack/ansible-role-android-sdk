---
android_sdk_packages:
  - platform-tools # must be first
  - cmdline-tools;latest # must be second
  - extras;android;m2repository # will be added by android-studio if absent
  - extras;google;m2repository # will be added by android-studio if absent
  - extras;google;google_play_services
  #- system-images;android-30;google_apis_playstore;x86 # Google Play ready image
  - build-tools;31.0.0
  - platforms;android-30
  - platforms;android-31
  - sources;android-30
  - sources;android-31
